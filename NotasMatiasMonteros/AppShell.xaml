<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NotasMatiasMonteros.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NotasMatiasMonteros"
    Title="Matias Monteros">

    <!-- Página principal -->
    <FlyoutItem Title="Notas" Icon="main.png">
        <ShellContent Title="Inicio" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <!-- sobre mí -->
    <FlyoutItem Title="Sobre mí" Icon="about.png">
        <ShellContent Title="Sobre mí" ContentTemplate="{DataTemplate local:AboutPage}" />
    </FlyoutItem>

</Shell>
